.ONESHELL:-
-:
	. ~/.nvm/nvm.sh && nvm exec npm install -g --force @pnpm/exe && nvm exec npm install -g node-pre-gyp && nvm exec npm install wrtc &&  nvm exec pnpm install && nvm exec pnpm rb
builder:
	. ~/.nvm/nvm.sh && nvm exec pnpm run builder
