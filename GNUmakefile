.ONESHELL:-
-:
	. ~/.nvm/nvm.sh && nvm exec npm install -g --force @pnpm/exe && nvm exec npm install --force wrtc && nvm exec npm install -g --force node-pre-gyp && nvm exec pnpm install && nvm exec pnpm rb
builder:
	. ~/.nvm/nvm.sh && nvm exec pnpm run builder
